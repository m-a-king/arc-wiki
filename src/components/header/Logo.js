import * as React from 'react';
import { Button } from '@mui/material';

export default function Logo() {
  return (
    <Button
      href="/"
      variant="text"
      color="inherit"
      size="small"
    >
      ARC'WIKI
    </Button>
  );
}